# Agent Instructions

This repository contains agent skills. Each skill is a self-contained folder under `skills/` with a `SKILL.md` file.

## Structure

```
skills/
  <skill-name>/
    SKILL.md       # Skill definition (frontmatter + instructions)
    scripts/       # Optional helper scripts
```
